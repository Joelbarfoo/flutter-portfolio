name: project_01
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:

  bloc: ^9.0.0
  flutter_bloc: ^9.1.1
  flutter:
    sdk: flutter
  dartz: ^0.10.1
  http: ^1.4.0
  get_it: ^8.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
